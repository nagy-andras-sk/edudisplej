# EduDisplej Configuration File
# This file stores the current configuration for the EduDisplej kiosk system

# Operating mode: EDSERVER, STANDALONE
MODE=EDSERVER

# URL to display in kiosk mode
KIOSK_URL=https://www.edudisplej.sk/edserver/demo/client

# Language: en (English) / sk (Slovak without diacritics)
LANG=sk

# Flag indicating if required packages are installed
PACKAGES_INSTALLED=0
