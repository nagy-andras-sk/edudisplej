# TR2 File Server Configuration
# This is a sample .env file. Copy this to /var/www/.env and configure your settings.

# Main server URL
TR2_MAIN_SERVER=https://tr.nagyandras.sk

# Pairing ID for this file server (unique identifier)
# Generate a random ID: md5(uniqid(rand(), true))
TR2_PAIRING_ID=

# Public IP and port (will be auto-detected if not set)
# Format: IP:PORT (e.g., 94.27.166.162:29715)
TR2_PUBLIC_IP=

# Heartbeat interval in seconds (default: 30)
TR2_HEARTBEAT_INTERVAL=30

# UPnP settings (set to false when running in Docker)
TR2_UPNP_ENABLED=false

# qBittorrent settings
QBIT_URL=http://localhost:8080
QBIT_USERNAME=admin
QBIT_PASSWORD=adminadmin

# Logging
LOG_LEVEL=INFO
